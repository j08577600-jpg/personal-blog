# 标签聚合页 执行计划

> 日期：`2026-03-23`
> 任务：标签聚合页
> 负责人：builder-codex
> 状态：✅ 已完成（P5 测试通过）

本执行计划是活文档，实施过程中必须持续更新。

## 目标

用户可通过 `/blog/tags` 浏览所有标签及文章数，点击任意标签进入 `/blog/tags/[tag]` 查看该标签下的所有已发布文章。`PostCard` 中的标签变为可点击链接。

## 上下文

- 方案设计：[`.agent/tasks/2026-03-23_标签聚合页/方案设计.md`](./方案设计.md)
- 相关文件：
  - `src/lib/posts.ts` — 新增 `getAllTags()`
  - `src/app/blog/tags/page.tsx` — 新增标签索引页
  - `src/app/blog/tags/[tag]/page.tsx` — 新增标签详情页
  - `src/components/post-card.tsx` — 标签改为可点击链接
  - `src/lib/site.ts` — 复用 `siteConfig.siteUrl` 生成 canonical URL
- 已知约束：
  - V1 内容源只走 `content/posts/*.mdx`，禁止引入数据库层
  - Server Component 优先，无需 client JS
  - 每次请求读文件系统（V1 体量可忽略）

## 验收标准

- [x] `npm run lint` 通过，无 error
- [x] `npm run build` 通过，无 type/build error
- [ ] 访问 `/blog/tags` 显示所有标签列表，每个标签带文章数，标签可点击
- [ ] 访问 `/blog/tags/[存在的标签]` 显示该标签下所有已发布文章，页面 title/description 正确
- [ ] 访问 `/blog/tags/[不存在的标签]` 返回 404（`notFound()`）
- [ ] `PostCard` 中标签样式不变，但变为可点击链接
- [ ] 两个页面均生成正确 canonical URL（SEO）

## 进度

- [x] Step 1: 新增 `getAllTags()` lib 函数
- [x] Step 2: 新增 `/blog/tags` 标签索引页
- [x] Step 3: 新增 `/blog/tags/[tag]` 标签详情页
- [x] Step 4: 更新 `PostCard` 组件，标签链接化
- [x] Step 5: 运行 `npm run lint` 和 `npm run build`
- [x] Step 6: 手动功能验证（✅ P5 测试通过）

**Commit:** `73004aa` — fix: add tag page to navigation and back link for UX consistency

## 决策日志

- Decision: 标签 URL 使用原始 tag 字符串而非 slugify
  原因：避免大小写/空格 normalization 引入歧义；URL 自动编码可处理特殊字符
  日期/作者：2026-03-23 / planner-codex

- Decision: PostCard 标签改为 Link 内联，不拆出新组件
  原因：改动最小，样式复用，无需引入额外复杂度
  日期/作者：2026-03-23 / planner-codex

## 意外与发现

- 发现：`src/lib/posts.ts` 末尾包含一个 `PostCard` 定义（与 `src/components/post-card.tsx` 内容相同，为冗余/遗留代码）。实际被页面导入使用的是 `src/components/post-card.tsx`。更新的是组件文件，posts.ts 中的副本未动（避免范围蔓延）。
- Decision: PostCard 中的 `span` 改 `Link` 无需引入 `"use client"`，因为 `Link` 组件在 Next.js App Router 中可从 Server Component 内联使用，无需 directive。
  日期/作者：2026-03-23 / builder-codex

## 中断恢复方式

如果在任意步骤中断，下次从这里恢复：

- **Step 1 中断**：从 `src/lib/posts.ts` 新增 `getAllTags()` 函数继续
- **Step 2 中断**：在 `src/app/blog/tags/page.tsx` 继续标签索引页实现
- **Step 3 中断**：在 `src/app/blog/tags/[tag]/page.tsx` 继续标签详情页实现
- **Step 4 中断**：在 `src/components/post-card.tsx` 继续 PostCard 更新
- **Step 5/6 中断**：从 lint/build 或功能验证步骤继续

## 实施步骤

### Step 1: 新增 `getAllTags()` lib 函数

**文件：** `src/lib/posts.ts`

**内容：** 在文件末尾（`getAllTags()` 和 `getAllValidPosts()` 之后）新增：

```typescript
export type TagEntry = {
  tag: string;
  count: number;
};

export function getAllTags(): TagEntry[] {
  const posts = getPosts(); // 仅已发布
  const counts: Record<string, number> = {};
  for (const post of posts) {
    for (const tag of post.tags) {
      counts[tag] = (counts[tag] ?? 0) + 1;
    }
  }
  return Object.entries(counts)
    .map(([tag, count]) => ({ tag, count }))
    .sort((a, b) => b.count - a.count || a.tag.localeCompare(b.tag));
}
```

**预期结果：** `getAllTags()` 返回按文章数降序排列的标签数组，文章数为 0 的标签不出现（因为只从已发布文章提取）

---

### Step 2: 新增标签索引页

**文件：** `src/app/blog/tags/page.tsx`（新建目录和文件）

**内容：**

```typescript
import type { Metadata } from "next";
import Link from "next/link";
import { getAllTags } from "@/lib/posts";

export const metadata: Metadata = {
  title: "标签",
  description: "按标签浏览博客文章。",
  alternates: { canonical: "/blog/tags" },
};

export default function TagsPage() {
  const tags = getAllTags();

  return (
    <div className="mx-auto max-w-5xl px-6 py-20">
      <div className="mb-12 max-w-2xl">
        <p className="mb-4 text-sm uppercase tracking-[0.22em] text-black/38 dark:text-white/38">标签</p>
        <h1 className="text-4xl font-semibold tracking-tight sm:text-5xl">按标签浏览</h1>
      </div>
      {tags.length === 0 ? (
        <p className="py-12 text-center text-sm text-black/45 dark:text-white/45">暂无标签。</p>
      ) : (
        <div className="flex flex-wrap gap-3">
          {tags.map(({ tag, count }) => (
            <Link
              key={tag}
              href={`/blog/tags/${encodeURIComponent(tag)}`}
              className="flex items-center gap-2 rounded-full border border-black/8 bg-white px-4 py-2 text-sm text-black/65 transition hover:border-black/20 dark:border-white/10 dark:bg-white/[0.02] dark:text-white/65 dark:hover:border-white/20"
            >
              <span>{tag}</span>
              <span className="text-xs text-black/35 dark:text-white/35">({count})</span>
            </Link>
          ))}
        </div>
      )}
    </div>
  );
}
```

**预期结果：** 页面渲染正常，显示所有标签及文章数，点击跳转到对应详情页

---

### Step 3: 新增标签详情页

**文件：** `src/app/blog/tags/[tag]/page.tsx`（新建目录和文件）

**内容：**

```typescript
import type { Metadata } from "next";
import { notFound } from "next/navigation";
import { PostCard } from "@/components/post-card";
import { getAllTags, getPosts } from "@/lib/posts";
import { siteConfig } from "@/lib/site";

interface Props {
  params: Promise<{ tag: string }>;
}

export async function generateStaticParams() {
  const tags = getAllTags();
  return tags.map(({ tag }) => ({ tag: encodeURIComponent(tag) }));
}

export async function generateMetadata({ params }: Props): Promise<Metadata> {
  const { tag } = await params;
  const decodedTag = decodeURIComponent(tag);
  const allTags = getAllTags();
  if (!allTags.find((t) => t.tag === decodedTag)) {
    return { title: "标签未找到" };
  }
  const posts = getPosts().filter((p) => p.tags.includes(decodedTag));
  const url = `${siteConfig.siteUrl}/blog/tags/${encodeURIComponent(tag)}`;

  return {
    title: `标签：${decodedTag}`,
    description: `浏览所有带有「${decodedTag}」标签的文章，共 ${posts.length} 篇。`,
    alternates: { canonical: url },
  };
}

export default async function TagPage({ params }: Props) {
  const { tag } = await params;
  const decodedTag = decodeURIComponent(tag);
  const allTags = getAllTags();

  if (!allTags.find((t) => t.tag === decodedTag)) {
    notFound();
  }

  const posts = getPosts().filter((p) => p.tags.includes(decodedTag));

  return (
    <div className="mx-auto max-w-5xl px-6 py-20">
      <div className="mb-12 max-w-2xl">
        <p className="mb-4 text-sm uppercase tracking-[0.22em] text-black/38 dark:text-white/38">标签</p>
        <h1 className="text-4xl font-semibold tracking-tight sm:text-5xl">
          {decodedTag}
          <span className="ml-3 text-2xl font-normal text-black/35 dark:text-white/35">({posts.length})</span>
        </h1>
      </div>
      {posts.length === 0 ? (
        <p className="py-12 text-center text-sm text-black/45 dark:text-white/45">该标签下暂无已发布文章。</p>
      ) : (
        <div className="grid gap-5">
          {posts.map((post) => (
            <PostCard key={post.slug} post={post} />
          ))}
        </div>
      )}
    </div>
  );
}
```

**预期结果：** 标签详情页正常显示文章列表，不存在的标签返回 404，SEO metadata 正确

---

### Step 4: 更新 PostCard 组件，标签链接化

**文件：** `src/components/post-card.tsx`

**改动：** 将标签渲染从 `<span>` 改为 `<Link>`，内联加 `"use client"`（如尚未存在）。

**改动前：**
```tsx
<div className="flex flex-wrap gap-2">
  {post.tags.map((tag) => (
    <span
      key={tag}
      className="rounded-full border border-black/8 px-2.5 py-1 text-xs text-black/55 dark:border-white/10 dark:text-white/55"
    >
      {tag}
    </span>
  ))}
</div>
```

**改动后：**
```tsx
<div className="flex flex-wrap gap-2">
  {post.tags.map((tag) => (
    <Link
      key={tag}
      href={`/blog/tags/${encodeURIComponent(tag)}`}
      className="rounded-full border border-black/8 px-2.5 py-1 text-xs text-black/55 transition hover:border-black/20 dark:border-white/10 dark:text-white/55 dark:hover:border-white/20"
    >
      {tag}
    </Link>
  ))}
</div>
```

**预期结果：** `PostCard` 中标签变为可点击链接，样式基本不变

---

### Step 5: lint & build

**命令：**
```bash
npm run lint
npm run build
```

**预期结果：** 全部通过，无 error

---

### Step 6: 手动功能验证

**验证项：**
1. 访问 `/blog/tags`，确认标签列表渲染（无文章时显示"暂无标签"）
2. 点击任意标签，确认跳转到 `/blog/tags/[tag]` 并显示对应文章
3. 访问不存在的标签（如 `/blog/tags/不存在的标签`），确认返回 404
4. 检查页面 `<title>` 和 `<meta name="description">` 是否正确
5. 检查 `PostCard` 中的标签是否可点击，链接地址是否正确
