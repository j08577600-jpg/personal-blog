# 标签聚合页 执行计划

> 日期：2026-03-23
> 任务：标签聚合页
> 负责人：builder-codex
> 状态：进行中

## 目标

用户访问 `/tags` 看到所有标签及文章数，点击标签进入 `/tags/[tag]` 查看该标签下所有文章。

## 上下文

- 相关文件：`src/lib/posts.ts`、`src/app/blog/page.tsx`、`src/components/post-card.tsx`
- 已知约束：只展示已发布文章，不暴露草稿；复用现有组件风格

## 验收标准

- [ ] `/tags` 页面正常渲染，显示所有标签和文章数
- [ ] `/tags/[tag]` 页面正常渲染，显示该标签下的文章列表
- [ ] 不存在的标签访问返回 404
- [ ] 导航栏显示「标签」链接
- [ ] `npm run lint` 通过
- [ ] `npm run build` 成功

## 进度

- [ ] 步骤 1：添加工具函数
- [ ] 步骤 2：创建 /tags 页面
- [ ] 步骤 3：创建 /tags/[tag] 页面
- [ ] 步骤 4：导航栏添加标签入口
- [ ] 步骤 5：lint + build
- [ ] 步骤 6：提交

## 实施步骤

### Step 1: 添加工具函数到 posts.ts
- 文件：`src/lib/posts.ts`
- 添加 `getAllTags()` 和 `getPostsByTag()`
- 预期：两个函数可正确返回标签数据和过滤文章

### Step 2: 创建 /tags 页面
- 文件：`src/app/tags/page.tsx`
- 显示所有标签列表（标签名 + 文章数），可点击跳转
- 预期：页面正常渲染，标签可点击

### Step 3: 创建 /tags/[tag] 页面
- 文件：`src/app/tags/[tag]/page.tsx`
- 显示该标签下的文章列表，使用 PostCard
- 预期：文章列表正常显示，不存在的标签 404

### Step 4: 导航栏添加标签入口
- 文件：`src/components/site-header.tsx`
- 在博客和关于之间添加「标签」链接
- 预期：导航栏显示「标签」

### Step 5: lint + build
- 命令：`npm run lint && npm run build`
- 预期：全部通过

### Step 6: 提交
- 提交信息：`feat: add tag aggregation pages (/tags and /tags/[tag])`
