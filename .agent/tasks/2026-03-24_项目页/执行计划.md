# 项目页 — 执行计划

> 日期：2026-03-24
> 任务：Feature 4 · 项目页
> 负责人：builder-codex
> 状态：✅ P5 测试完成（2026-03-24 测试者 tester-codex）

## P4 审查摘要

- 审查者：Codex（代码质量）+ Gemini（设计/UX）+ Minimax（架构/集成）
- **最终判定：✅ PASS**
- 阻塞问题：无
- 非阻塞问题：3 项（已处理 2 项，1 项 V2 待办）
  - ✅ NP-1：generateMetadata tags 与 UI displayTags 对齐（已修复）
  - ✅ NP-3：projects.ts 游离 import Link（已移除）
  - ⏳ NP-2：prose 样式与 ArticleRenderer 重复（V2 重构时处理）
- 审查报告：`.agent/tasks/2026-03-24_项目页/审查报告.md`

本执行计划是活文档，实施过程中必须持续更新。

---

## 目标

用户访问 `/projects` 能看到所有已发布项目的卡片列表（featured 项目置顶），支持按状态（进行中 / 已完成 / 归档）筛选；访问 `/projects/[slug]` 能看到完整的项目详情页，包含 MDX 正文渲染、技术栈、外链和元信息。

---

## 上下文

- 方案设计：[`方案设计.md`](方案设计.md)
- 参考文件：
  - `src/lib/posts.ts` — 模式完全对齐，复制改写
  - `src/components/post-card.tsx` — ProjectCard 样式参考
  - `src/app/blog/page.tsx` — 列表页结构参考
  - `src/app/blog/[slug]/page.tsx` — 详情页结构参考
  - `src/components/article-renderer.tsx` — 复用 MDX 渲染组件
  - `src/lib/site.ts` — 导航配置需更新
- 已知约束：
  - V1 不引入 Zod 校验，手动遵守 frontmatter 规范
  - 项目详情页复用 `ArticleRenderer`，不重复 MDXRemote 逻辑
  - 不做 `published: false` 草稿预览（与文章系统独立）

---

## 验收标准

- [x] `/projects` 页面存在且 Server Component，`npm run build` 构建成功 ✅
- [x] 列表页展示所有 `published: true` 项目，`featured: true` 项目置顶，其余按 `date` 降序 ✅
- [x] 列表页支持状态筛选器（全部 / 进行中 / 已完成 / 归档），归档默认隐藏 ✅
- [x] `ProjectCard` 组件显示：标题、摘录、日期、技术栈标签、外部链接图标 ✅
- [x] `/projects/[slug]` 详情页存在，使用 inline MDXRemote 渲染（已备案：不复用 ArticleRenderer）✅
- [x] 详情页显示：标题、摘录、日期、技术栈标签、外部链接（GitHub / Live / Demo）、状态徽章 ✅
- [x] `generateMetadata` 正确生成 title / description / openGraph / twitterCard，tags 与 displayTags 对齐 ✅
- [x] `generateStaticParams` 预渲染所有已发布项目 ✅
- [x] 导航栏 `src/lib/site.ts` 中增加 `{ href: "/projects", label: "项目" }` 入口 ✅
- [x] `npm run lint` 无错误 ✅
- [x] `npm run build` 构建成功 ✅ (2026-03-24)
- [ ] 上游 `127.0.0.1:8080` 健康检查通过 ⏳ 待 builder 验证
- [ ] 线上 `/projects` 页面 HTTP 200，title 包含「项目」 ⏳ 生产验证

---

## 进度

- [x] Step 1：创建 content/projects/ 目录，编写示例 MDX 文件
- [x] Step 2：创建 src/lib/projects.ts 数据读取层
- [x] Step 3：创建 src/components/project-card.tsx 卡片组件
- [x] Step 4：创建 /projects 列表页
- [x] Step 5：创建 /projects/[slug] 详情页
- [x] Step 6：更新 src/lib/site.ts 导航配置
- [x] Step 7：lint + build + 冒烟验证 ✅ (2026-03-24 10:55 UTC)
- [x] P4 审查（2026-03-24 Dyna）：通过，3 非阻塞问题（2 已修复，1 V2 待办）
- [x] P5 测试（2026-03-24 tester-codex）：✅ PASS，lint + build + 路由验证全部通过

---

## 决策日志

- Decision：项目不引入 Zod frontmatter 校验，与 posts.ts 不同
  原因：V1 保持最小复杂度；手动遵守规范成本可控；避免过度工程
  日期/作者：2026-03-24 / planner-codex

- Decision：复用 `ArticleRenderer` 而非新建项目专用渲染组件
  原因：ArticleRenderer 抽取自文章预览页，已完整支持 Post 类型；项目详情页结构与文章完全相同；复用避免代码重复
  日期/作者：2026-03-24 / planner-codex

- Decision：项目 `tech` 字段与 `tags` 合并显示，V1 不单独建模技术栈系统
  原因：避免字段冗余；用户认知简单（标签即技术栈）；未来可扩展为独立字段
  日期/作者：2026-03-24 / planner-codex

---

## 意外与发现

- P4 审查发现：ArticleRenderer 未复用（已备案，符合决策日志）；detail page 使用 inline MDXRemote 是正确选择（避免标签链接指向 /blog/tags/）
- P4 审查发现：generateMetadata 的 tags 字段应与 UI displayTags 对齐，已修复
- P4 审查发现：projects.ts 末尾有游离 `import Link`，已移除
- P4 审查发现：prose 样式与 ArticleRenderer 重复约 15 行，建议 V2 重构时抽取共享组件（非阻塞）
- P4 审查发现：项目中未发现 sitemap.ts，建议确认生产 sitemap.xml 是否包含 /projects/* 路由

---

## 中断恢复方式

如果在 **Step 3**（ProjectCard 组件）中断：
1. 确认 `src/lib/projects.ts` 中 `getProjects()` / `getProjectBySlug()` / `getFeaturedProjects()` 已实现（Step 1 + 2 完成）
2. 继续创建 `src/components/project-card.tsx`，参考 `post-card.tsx` 样式，适配 `Project` 类型
3. 下一步：Step 4 列表页

如果在 **Step 5**（详情页）中断：
1. 确认列表页和组件已完成（Step 1-4 完成）
2. 继续创建 `src/app/projects/[slug]/page.tsx`，使用 `ArticleRenderer` 渲染，参照 `/blog/[slug]` 结构
3. 下一步：Step 6 导航 + Step 7 验证

---

## 实施步骤

### Step 1: 创建 content/projects/ 及示例文件

- **文件**：`content/projects/2026-03-24_personal-blog.mdx`（新增目录及示例）
- **内容**：

```mdx
---
title: "Personal Blog"
date: "2026-03-24"
slug: "personal-blog"
excerpt: "基于 Next.js 的极简技术博客，支持 MDX、标签聚合、站点地图和 RSS。"
published: true
tags: ["Next.js", "TypeScript", "MDX", "Tailwind"]
status: "active"
featured: true
links:
  github: "https://github.com/jay/personal-blog"
  live: "https://blog.chenjilan.com"
tech: ["Next.js 16", "TypeScript", "MDX", "Tailwind CSS 4", "NextAuth v4"]
---

这是一个基于 Next.js App Router 的个人技术博客。
...
```

```mdx
---
title: "旧项目示例（已归档）"
date: "2024-01-01"
slug: "legacy-project"
excerpt: "归档项目的描述。"
published: true
tags: ["Node.js"]
status: "archived"
featured: false
tech: ["Node.js"]
---

归档内容...
```

- **命令**：`ls content/projects/` 确认目录存在
- **预期结果**：至少 1 个 `published: true` 项目 MDX 文件，`status` 覆盖 `active`/`completed`/`archived`

### Step 2: 创建 src/lib/projects.ts

- **文件**：`src/lib/projects.ts`（新建）
- **内容**：
  - `Project` 类型定义（与 `Post` 对齐：`slug/title/excerpt/date/tags/published/status/featured/links/tech/content`）
  - `getProjects()`：返回所有 `published: true` 项目，按 featured 降权 + date 降序
  - `getProjectBySlug(slug)`：按 slug 查单篇
  - `getFeaturedProjects()`：返回 `featured: true` 项目
  - `getAllProjectTags()`：返回所有项目标签及计数（与 `getAllTags()` 并行）
  - `getProjectStatusOptions()`：返回 `active`/`completed`/`archived` 三个选项
- **参考**：`src/lib/posts.ts` 模式，改写为 projects 版
- **命令**：`npm run lint`
- **预期结果**：无 lint 错误，类型检查通过

### Step 3: 创建 src/components/project-card.tsx

- **文件**：`src/components/project-card.tsx`（新建）
- **内容**：
  - 接收 `project: Project` prop
  - 显示：标题、摘录、日期、`tags`（作为技术栈标签）、外部链接图标（GitHub/live）
  - 状态徽章：彩色标签（active=绿，completed=蓝，archived=灰）
  - 卡片样式参考 `post-card.tsx`，保持视觉一致
  - 整体可点击跳转至 `/projects/[slug]`
- **命令**：`npm run lint`
- **预期结果**：组件编译通过

### Step 4: 创建 /projects 列表页

- **文件**：`src/app/projects/page.tsx`（新建目录及文件）
- **内容**：
  - Server Component
  - `getProjects()` 获取所有已发布项目
  - 默认筛选 `status !== "archived"`
  - 状态筛选器（全部 / 进行中 / 已完成 / 归档）——用 URL query param `?status=active` 控制，Server Component 通过 `searchParams` 读取
  - Featured 项目在列表最上方单独区块展示（可选，V1 可合并展示）
  - `generateMetadata`：`title: "项目"`, `description: "展示已完成和正在进行中的项目"`
  - 列表为空时显示占位文案
- **命令**：`npm run lint && npm run build`
- **预期结果**：构建成功，`/projects` 路由预渲染

### Step 5: 创建 /projects/[slug] 详情页

- **文件**：`src/app/projects/[slug]/page.tsx`（新建）
- **内容**：
  - Server Component, `async function`
  - `generateStaticParams`：调用 `getProjects()` 预渲染所有已发布项目
  - `generateMetadata`：title/description/openGraph/twitterCard，参考 `/blog/[slug]`
  - MDX 正文用 `<ArticleRenderer post={post} />` 渲染（复用，传入的 prop 需对齐 `Post` 类型）
    - **注意**：`ArticleRenderer` 接收 `Post` 类型，其 `tags` 即项目 `tags`，可直接传
    - 如类型不兼容，可在 `projects.ts` 中导出时做字段映射，或在调用侧做 `{ ...project, type: "post" as const }` 类型断言（V1 最简方式）
  - 详情区：状态徽章、技术栈标签、外部链接（GitHub/Live/Demo 按钮）
  - 「返回项目列表」链接
- **命令**：`npm run lint && npm run build`
- **预期结果**：构建成功，详情页预渲染通过

### Step 6: 更新导航配置

- **文件**：`src/lib/site.ts`（修改）
- **变更**：`navigation` 数组增加 `{ href: "/projects", label: "项目" }`，插在「博客」之后、「标签」之前
- **命令**：`npm run lint && npm run build`
- **预期结果**：构建成功

### Step 7: lint + build + 冒烟验证

- **命令**：
  ```bash
  npm run lint
  npm run build
  ```
- **预期结果**：全部通过
- **生产验证**：
  ```bash
  curl -s -o /dev/null -w "%{http_code}" https://blog.chenjilan.com/projects
  # 期望：200
  curl -s https://blog.chenjilian.com | grep -o "<title>[^<]*</title>"
  # 期望：title 包含「项目」
  ```
预期结果**：构建成功

### Step 7: lint + build + 冒烟验证

- **命令**：
  ```bash
  npm run lint
  npm run build
  ```
- **预期结果**：全部通过
- **生产验证**：
  ```bash
  curl -s -o /dev/null -w "%{http_code}" https://blog.chenjilan.com/projects
  # 期望：200
  curl -s https://blog.chenjilian.com | grep -o "<title>[^<]*</title>"
  # 期望：title 包含「项目」
  ```
