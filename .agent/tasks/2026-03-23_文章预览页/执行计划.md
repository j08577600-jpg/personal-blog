# 文章预览页 — 执行计划

> 日期：2026-03-23
> 任务：Feature C · 文章预览页
> 负责人：builder-codex
> 状态：待实施

本执行计划是活文档，实施过程中必须持续更新。

---

## 目标

用户（已登录且已授权的作者）访问 `/dashboard/preview/[slug]`，能看到该 slug 对应草稿文章的完整渲染效果，包括正文、标签、阅读时间等，与正式文章页视觉完全一致。

---

## 上下文

- 方案设计：[`.agent/tasks/2026-03-23_文章预览页/方案设计.md`](方案设计.md)
- 依赖文件：
  - `src/app/blog/[slug]/page.tsx` — 当前文章渲染页（需抽取组件）
  - `src/lib/posts.ts` — `getPostBySlug` 已支持返回草稿（不区分 published）
  - `src/lib/auth.ts` — authOptions 用于会话校验
  - `src/components/post-card.tsx` — 样式参考

---

## 验收标准

- [ ] `src/app/dashboard/preview/[slug]/page.tsx` 存在且为 Server Component
- [ ] 未登录用户访问 `/dashboard/preview/xxx` 跳转 `/login`
- [ ] 未授权用户（whitelisted=false）访问跳转 `/unauthorized`
- [ ] 存在 slug 对应的草稿文章时，完整渲染 MDX，显示标签、日期、阅读时间
- [ ] slug 对应已发布文章或不存在时，返回 404（`notFound()`）
- [ ] 页面顶部有「返回控制台」和「返回博客」链接
- [ ] `generateMetadata` 正确生成 `<title>` 和 `<meta description>`
- [ ] `npm run lint` 无错误
- [ ] `npm run build` 构建成功
- [ ] `/dashboard/preview/draft-test-zuopin-kuangjia-sikao` 能正确预览 `2026-03-23_draft-test-zuopin-kuangjia-sikao.mdx`（published: false）

---

## 实施步骤

### Step 1: 抽取 ArticleRenderer 组件

- **文件**：`src/components/article-renderer.tsx`（新建）
- **内容**：将 `src/app/blog/[slug]/page.tsx` 中的文章主体渲染部分抽取为独立组件 `ArticleRenderer`
  - 接收 `post: Post` 作为 prop
  - 包含：标题区、日期/阅读时间、标签、MDXRemote 渲染
  - **不包含**：「返回博客」链接（各页面自行决定是否加）
- **命令**：`npm run lint`
- **预期结果**：新组件编译通过，无 lint 错误

### Step 2: 重构 `/blog/[slug]` 使用 ArticleRenderer

- **文件**：`src/app/blog/[slug]/page.tsx`（修改）
- **变更**：移除内联渲染逻辑，改为导入并使用 `<ArticleRenderer post={post} />`
- **命令**：`npm run lint && npm run build`
- **预期结果**：公开文章页行为不变，构建通过

### Step 3: 创建预览页路由

- **文件**：`src/app/dashboard/preview/[slug]/page.tsx`（新建）
- **内容**：
  1. Server Component，`async function`
  2. `getServerSession(authOptions)` 校验登录态，未登录 `redirect("/login")`
  3. 校验 whitelisted，未授权 `redirect("/unauthorized")`
  4. `getPostBySlug(slug)` 获取文章，若不存在或 published 为 true，调用 `notFound()`
  5. 顶部「返回控制台 →」和「← 返回博客」链接
  6. 使用 `<ArticleRenderer post={post} />` 渲染内容
  7. `generateMetadata`：返回 `title: post.title + "（预览）"`
- **命令**：`npm run lint && npm run build`
- **预期结果**：预览页构建成功，行为符合验收标准

### Step 4: 更新 dashboard PostRow 为草稿添加预览链接

- **文件**：`src/app/dashboard/page.tsx`（修改）
- **变更**：在 `PostRow` 组件的草稿状态分支，将 `<span>草稿</span>` 替换为 `<Link href={/dashboard/preview/${post.slug}}>预览 →</Link>`
- **命令**：`npm run lint && npm run build`
- **预期结果**：dashboard 中草稿行出现可点击的「预览 →」链接

---

## 进度

- [x] Step 1：抽取 ArticleRenderer 组件
- [x] Step 2：重构 /blog/[slug] 使用 ArticleRenderer
- [x] Step 3：创建 /dashboard/preview/[slug] 路由
- [x] Step 4：更新 dashboard 草稿行添加预览链接

---

## 决策日志

- Decision：预览页放在 `/dashboard/preview/[slug]` 而非 `/blog/preview/[slug]`
  原因：预览是作者行为，与 dashboard 同属受保护区域；避免污染公开博客路由；语义正确
  日期/作者：2026-03-23 / planner-codex

- Decision：草稿文章（published=false）即使 slug 匹配也视为不存在，不允许预览
  原因：已发布文章有正式页面，无需预览；避免路由歧义
  日期/作者：2026-03-23 / planner-codex

- Decision：预览页 auth 失败时使用 `notFound()` 而非 `redirect("/login")` / `redirect("/unauthorized")`
  原因：避免泄露受保护路由的存在；`notFound()` 是标准 Next.js 行为，不会给未授权用户任何提示
  日期/作者：2026-03-23 / builder-codex

---

## 中断恢复方式

如果在 **Step 3**（预览页路由创建）中断，下次从这里恢复：
1. 确认 `ArticleRenderer` 组件已正确抽取（Step 1 + Step 2 完成）
2. 继续创建 `src/app/dashboard/preview/[slug]/page.tsx`，按步骤要求实现
3. 确认 auth 守卫、notFound 处理、metadata 均正确

如果在 **Step 4** 中断，恢复后补充 dashboard PostRow 的预览链接即可，前三步无依赖。

---

## 实施记录（builder 更新）

> builder 在每个停止点填写以下内容。

### Step 1 完成记录

- 完成时间：2026-03-23
- 实际结果：成功抽取 `src/components/article-renderer.tsx`，包含文章主体渲染逻辑（标题区、日期/阅读时间、标签、MDXRemote body）。不含 back-link，由各页面自行处理。
- 与预期差异：无
- 下一步：Step 2

### Step 2 完成记录

- 完成时间：2026-03-23
- 实际结果：`src/app/blog/[slug]/page.tsx` 重构为使用 `<ArticleRenderer post={post} />`，移除了内联渲染代码和 MDXRemote 导入。lint 通过 ✓
- 与预期差异：无
- 下一步：Step 3

### Step 3 完成记录

- 完成时间：2026-03-23
- 实际结果：
  - 创建 `src/app/dashboard/preview/[slug]/page.tsx`
  - Auth 检查：未登录 → `notFound()`（避免泄露路由存在）
  - Whitelisted 检查：未授权 → `notFound()`
  - Post 加载：不存在或 `published=true` → `notFound()`
  - 顶部导航：← 返回控制台 / 返回博客 → + 草稿预览横幅
  - `generateMetadata`：`title = post.title + "（预览）"`
  - lint 通过 ✓
- 与预期差异：
  - Auth 失败时用 `notFound()` 而非 redirect；更安全，不泄露路由
- 下一步：Step 4

### Step 4 完成记录

- 完成时间：2026-03-23
- 实际结果：Dashboard `PostRow` 草稿分支从 `<span>草稿</span>` 替换为 `<Link href="/dashboard/preview/${post.slug}">预览 →</Link>`，与已发布「查看 →」风格一致。lint 通过 ✓
- 与预期差异：无
- 下一步：完成 P3，提交审查
