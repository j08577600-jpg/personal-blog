# 监控与日志轮转 执行计划

> 日期：`2026-03-24`
> 任务：监控与日志轮转
> 负责人：builder-codex
> 状态：待开始

本执行计划是活文档，实施过程中必须持续更新。

## 目标

- 部署后可通过 `curl https://blog.chenjilan.com/api/health` 获得结构化健康状态
- 日志自动轮转，不因日志无限增长导致磁盘满
- 运维人员有完整的监控自查清单

## 上下文

- 方案设计：`.agent/tasks/2026-03-24_监控与日志轮转/2026-03-24_监控与日志轮转_方案设计.md`
- 相关文件：
  - `src/app/api/health/route.ts`（新增）
  - `deploy/logrotate.conf`（新增）
  - `deploy/DEPLOY_RUNBOOK.md`（补充）
  - `/etc/systemd/journald.conf`（系统文件，补充配置）
- 已知约束：
  - 单服务器部署，systemd 管理进程
  - Next.js App Router，TypeScript strict
  - 健康端点不能依赖外部库
  - logrotate 需要 sudo 权限部署

## 验收标准

- [ ] `npm run lint` 通过
- [ ] `npm run build` 成功
- [ ] `curl http://127.0.0.1:8080/api/health` 返回 HTTP 200，body 包含 `{"ok":true,"timestamp":"...","version":"..."}`
- [ ] `curl http://127.0.0.1:8080/api/health` 在故意破坏场景下返回 HTTP 500（测试用，验收后可不保留）
- [ ] `logrotate --debug /etc/logrotate.d/personal-blog` dry-run 无报错
- [ ] 上游 `127.0.0.1:8080` 健康检查通过（`curl -fsSI --max-time 5 http://127.0.0.1:8080/api/health`）
- [ ] 线上域名健康检查通过（`curl -kfsSI --max-time 5 https://blog.chenjilan.com/api/health`）
- [ ] `systemctl --no-pager status personal-blog` 显示 active (running)
- [ ] `DEPLOY_RUNBOOK.md` 包含监控自查章节

## 进度

- [x] Step 1: 新增 `/api/health` 接口
- [x] Step 2: 配置 `logrotate` 轮转规则
- [x] Step 3: 调整 `journald.conf` 保留策略（补充配置说明）
- [x] Step 4: 补充 `DEPLOY_RUNBOOK.md` 监控自查章节
- [x] Step 5: 预发布验证（lint + build + health check）

## 实施记录

- 2026-03-24 / builder-codex:
  - 完成 Step 1-5，lint + build 全部通过
  - git commit: `0a55f0e`
  - `/api/health` 已出现在 Next.js build 输出中（Route (app) → ƒ /api/health）
  - lint warning `allPosts is assigned but never used` 是既有代码，与本次改动无关

## 决策日志

- Decision: 使用 `/api/health` API Route 而非独立 server
  原因：Next.js App Router 天然支持，无额外依赖，最小侵入
  日期/作者：2026-03-24 / planner-codex

- Decision: logrotate + journald.conf 双保险
  原因：单点 logrotate 可能漏跑，journald 配置兜底防止磁盘满
  日期/作者：2026-03-24 / planner-codex

## 意外与发现

- 观察：
  证据：
  日期：

## 中断恢复方式

如果在任意 Step 中断，下次从这里恢复：
- 确认当前 Step 的验收标准是否已满足
- 确认相关文件是否已创建/修改
- 继续执行下一个未完成的 Step

## 实施步骤

### Step 1: 新增 `/api/health` 接口

- 文件：`src/app/api/health/route.ts`
- 命令：无（由 builder-codex 创建文件 + lint + build）
- 预期结果：
  - `src/app/api/` 目录存在
  - 文件返回 `{ ok: true, timestamp: ISO string, version: "0.1.0" }`
  - Content-Type: application/json
  - HTTP 200

实现参考：

```typescript
// src/app/api/health/route.ts
import { NextResponse } from "next/server";

export const dynamic = "force-dynamic";

export async function GET() {
  return NextResponse.json(
    {
      ok: true,
      timestamp: new Date().toISOString(),
      version: process.env.npm_package_version ?? "0.1.0",
    },
    { status: 200 }
  );
}
```

> 注意：`dynamic = "force-dynamic"` 避免 Next.js 缓存，确保每次返回最新时间戳。

---

### Step 2: 配置 `logrotate` 轮转规则

- 文件：`deploy/logrotate.conf`（新建）
- 命令（部署时手动执行）：
  ```bash
  # 检查 journald 存储路径
  journalctl --system -n 1 --no-pager 2>/dev/null | head -5
  # 确认是否存在持久化目录
  ls /var/log/journal/ 2>/dev/null || echo "tmpfs mode"
  ```

预期结果：
- `deploy/logrotate.conf` 包含针对 journald 和 nginx 日志的轮转规则
- 文件语法通过 `logrotate --debug` 验证
- `sudo cp deploy/logrotate.conf /etc/logrotate.d/personal-blog`

`deploy/logrotate.conf` 参考内容：

```conf
# /etc/logrotate.d/personal-blog
# Next.js 应用日志轮转（systemd journal + Nginx）

# journald 日志轮转（如果有持久化目录）
/var/log/journal/*/ {
    weekly
    rotate 4
    compress
    delaycompress
    notifempty
    missingok
    maxsize 100M
}

/var/log/journal/*/*.journal {
    weekly
    rotate 4
    compress
    delaycompress
    notifempty
    missingok
}

# Nginx 日志轮转
/var/log/nginx/*.log {
    weekly
    rotate 4
    compress
    delaycompress
    notifempty
    missingok
    sharedscripts
    postrotate
        [ -f /var/run/nginx.pid ] && kill -USR1 $(cat /var/run/nginx.pid) || true
    endscript
}

/home/cjl/projects/personal-blog/logs/*.log {
    weekly
    rotate 4
    compress
    delaycompress
    notifempty
    missingok
    create 0644 cjl cjl
}
```

---

### Step 3: 调整 `journald.conf` 保留策略（配置说明）

此步骤**不修改系统文件**，而是提供配置说明，由运维手动执行（需要 sudo）。

- 文件：`deploy/journald.conf补丁说明.md`（新建，作为文档）
- 命令：无
- 预期结果：文档说明如何配置 journald 保留策略

核心配置项：
```ini
# /etc/systemd/journald.conf
[Journal]
SystemMaxUse=500M
SystemMaxFileSize=50M
MaxRetentionSec=14day
```

> 注意：如果 `/var/log/journal/` 不存在（tmpfs 模式），日志重启后丢失，可选择性创建持久化目录。

---

### Step 4: 补充 `DEPLOY_RUNBOOK.md` 监控自查章节

- 文件：`deploy/DEPLOY_RUNBOOK.md`
- 命令：无
- 预期结果：文件末尾追加「监控与日志」章节

追加内容：

```markdown
## 监控与日志

### 健康检查

```bash
# 上游健康（自动）
curl -fsSI --max-time 5 http://127.0.0.1:8080/api/health

# 线上健康（自动）
curl -kfsSI --max-time 5 https://blog.chenjilan.com/api/health
```

健康端点返回 `{ "ok": true, "timestamp": "...", "version": "0.1.0" }` 时视为正常。

### 日志查看

```bash
# Next.js 应用日志（最近 200 行）
journalctl -u personal-blog -n 200 --no-pager

# 实时跟踪
journalctl -u personal-blog -f --no-pager

# Nginx 错误日志
sudo tail -n 100 /var/log/nginx/error.log

# 日志磁盘占用
journalctl --disk-usage
du -sh /var/log/journal/ 2>/dev/null || echo "journal tmpfs mode"
```

### 日志轮转验证

```bash
# 手动触发 logrotate dry-run
sudo logrotate --debug /etc/logrotate.d/personal-blog

# 检查 logrotate cron 是否存在
ls /etc/cron.daily/logrotate 2>/dev/null && echo "OK" || echo "NOT FOUND"
```

### 常见故障

| 现象 | 检查项 |
|------|--------|
| 健康检查 500 | `journalctl -u personal-blog -n 50 --no-pager` |
| 磁盘满 | `journalctl --disk-usage`；`du -sh /var/log/*` |
| Nginx 502 | 见「故障排查」章节 |
```

---

### Step 5: 预发布验证

- 文件：无
- 命令：
  ```bash
  cd /home/cjl/projects/personal-blog
  npm run lint
  npm run build
  curl -fsSI --max-time 5 http://127.0.0.1:8080/api/health
  curl -kfsSI --max-time 5 https://blog.chenjilan.com/api/health
  systemctl --no-pager status personal-blog
  ```
- 预期结果：所有命令成功，无报错

## 部署后需手动执行的操作

以下操作需要 SSH 登录服务器 sudo 执行，不在 CI/CD 范围内：

```bash
# 1. 安装 logrotate 规则
sudo cp /home/cjl/projects/personal-blog/deploy/logrotate.conf /etc/logrotate.d/personal-blog

# 2. （可选）配置 journald 持久化
sudo mkdir -p /var/log/journal/$(id -u)
sudo systemd-tmpfiles --create --prefix /var/log/journal
# 编辑 /etc/systemd/journald.conf 添加保留策略

# 3. 重启服务
sudo systemctl restart personal-blog

# 4. 验证
curl -kfsSI https://blog.chenjilan.com/api/health
```
