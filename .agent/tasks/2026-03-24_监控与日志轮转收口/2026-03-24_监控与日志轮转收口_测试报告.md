# 监控与日志轮转收口 测试报告

> 日期：`2026-03-24`
> 任务：监控与日志轮转收口

## 测试范围

本次 P5 为文档收口任务验收，按 tester 规范将四类测试映射为以下检查：
- 单元测试 -> 命令可执行性检查
- 冒烟测试 -> 文档闭环检查
- 功能测试 -> 备份/回退/重跑与 journald/logrotate 边界一致性检查
- 回归测试 -> 脚本语法检查与流程回归检查

本次仅执行仓库内可验证项，不执行任何真实线上命令，不假设线上服务、systemd、journald、logrotate 已完成验收。

## 前置确认

- [✔] 已先阅读 `.agent/tasks/2026-03-24_监控与日志轮转收口/2026-03-24_监控与日志轮转收口_审查报告.md`
- [✔] 审查结论允许进入测试；三审要求的 Step 6 补盲已在当前仓库文件中落地
- [✔] 已阅读执行计划与相关收口文件：
  - `.agent/tasks/2026-03-24_监控与日志轮转收口/2026-03-24_监控与日志轮转收口_执行计划.md`
  - `deploy/DEPLOY_RUNBOOK.md`
  - `deploy/journald.conf补丁说明.md`
  - `deploy/logrotate.conf`
  - `scripts/release-check.sh`

## 测试结果

### 单元测试（命令可执行性检查）

- [✔] `bash -n scripts/release-check.sh` 通过，脚本无 bash 语法错误
- [✔] `sh -n scripts/release-check.sh` 通过，脚本基础 POSIX 语法兼容性未发现异常
- [✔] `deploy/logrotate.conf` 结构检查通过：4 个 stanza 均存在，花括号数量配对，无明显结构性缺失
- [ ] `sudo logrotate --debug /etc/logrotate.d/personal-blog` 真实执行结果待线上验证
- [ ] `journalctl --disk-usage`、`du -sh /var/log/journal` 真实执行结果待线上验证

### 冒烟测试（文档闭环检查）

- [✔] `deploy/DEPLOY_RUNBOOK.md` 已形成固定顺序闭环：上游健康 -> 公网健康 -> 服务状态 -> 应用日志 -> 日志轮转 -> 磁盘占用
- [✔] runbook 已写明 Step 0 前置条件、失败即停原则、每步通过标准、失败表现、恢复动作
- [✔] 发布流程与收口流程已衔接：重启服务后要求立即进入 Step 0，再按 Step 1 到 Step 6 完成闭环
- [✔] 文档已明确“本节只定义部署后检查闭环，不代表这些命令已在线上执行”，仓库内说明与线上证据边界清晰
- [✔] 原遗留项已区分为“被当前文件状态吸收”与“仍待真实线上验证”，未把原任务已完成项重新伪装成待开发

### 功能测试（闭环一致性检查）

- [✔] logrotate 与 journald 两类恢复动作均明确“先备份再覆盖/回退”，要求一致
- [✔] logrotate 与 journald 两类恢复动作均明确“恢复后从 Step 1 重跑完整闭环”，无局部签收口子
- [✔] `deploy/DEPLOY_RUNBOOK.md` Step 6 已补齐 `/etc/systemd/journald.conf` 备份要求，解决三审指出的不对称问题
- [✔] `deploy/journald.conf补丁说明.md` 已区分 `SIGHUP` 与 `restart` 的适用边界；涉及 `SystemMaxFileSize`、`MaxRetentionSec` 时以 `restart` 为保守口径
- [✔] journald 生效判定边界一致：文档都明确 `journalctl --disk-usage` 只能确认当前模式与占用，不能单独证明容量/保留策略已实际触发
- [✔] `deploy/logrotate.conf` 当前只包含 journald 与 Nginx 规则；与执行计划中“本轮不继续调整 logrotate 逻辑”的决策一致
- [ ] journald 单文件轮转、14 天保留是否在真实系统中按预期触发，需线上长期观察验证
- [ ] `/api/health` 上游、公网、systemd、应用日志、logrotate debug 的真实闭环结果，需线上逐步验证

### 回归测试（脚本语法检查与流程回归检查）

- [✔] `scripts/release-check.sh` 当前职责仍为发布前检查：`lint`、`build`、上游/public 健康探测、`systemctl` 状态检查；未被本轮收口意外改写
- [✔] 执行计划与当前文件保持一致：本轮不把部署后检查脚本化，不继续扩展 `scripts/release-check.sh`
- [✔] 执行计划与 runbook 保持一致：真实线上执行仍是独立阶段，不被仓库内文档收口替代
- [✔] 审查报告中的唯一必要补盲（Step 6 journald 恢复对称性）已闭合，P4 -> P5 流转条件满足
- [ ] `npm run lint`
- [ ] `npm run build`
- [ ] `curl -fsS http://127.0.0.1:8080/api/health`
- [ ] `curl -kfsS https://blog.chenjilan.com/api/health`
- [ ] `systemctl --no-pager --full status personal-blog`
- [ ] `journalctl -u personal-blog -n 200 --no-pager`

**状态含义：**
- 待测试：`[ ]`
- 通过：`[✔]`
- 失败：`[×]`

## 仓库内已验证项

- `scripts/release-check.sh` 可通过 shell 语法检查，当前脚本未出现仓库内可见的语法问题
- `deploy/DEPLOY_RUNBOOK.md` 已形成部署后收口闭环，且发布 -> 重启 -> Step 0 -> Step 1~6 的路径完整
- `deploy/DEPLOY_RUNBOOK.md` 与 `deploy/journald.conf补丁说明.md` 在备份、回退、重跑完整闭环、即时验收边界上的口径已对齐
- `deploy/logrotate.conf` 当前结构与执行计划中的“暂不继续开发”结论一致，未发现必须在本轮阻塞发布的仓库内结构问题
- 执行计划、审查报告、runbook、journald 说明之间的收口边界一致：仓库内负责说明闭环，线上负责产出真实证据

## 必须留到真实线上执行的未验证项

- `curl -fsS http://127.0.0.1:8080/api/health` 是否返回 HTTP 200 且 body 含 `"ok":true`
- `curl -kfsS https://blog.chenjilan.com/api/health` 是否返回 HTTP 200 且 body 含 `"ok":true`
- `systemctl --no-pager --full status personal-blog` 是否为 `active (running)` 且无 crash loop
- `journalctl -u personal-blog -n 200 --no-pager` 是否无新的启动失败、端口冲突、权限错误、重复重启
- `sudo logrotate --debug /etc/logrotate.d/personal-blog` 是否无语法错误、无路径级致命错误
- `journalctl --disk-usage` 与 `/var/log/journal` 占用是否在预期阈值内
- `SystemMaxFileSize=50M`、`MaxRetentionSec=14day` 是否在真实系统里按预期生效，仍需后续滚动/保留周期观察

## 风险项

- 风险 1：当前没有仓库内失败项，但发布闭环所需的强制线上证据尚未执行；若此时直接放行，会把“文档已收口”误当成“生产已验收”。
- 风险 2：`scripts/release-check.sh` 混合本地构建与线上探测，已知不适合作为本轮完整收口脚本；若临时把它当成部署后验收替代，会放大环境耦合误判。
- 风险 3：journald 容量与保留策略的真实触发需要时间或文件滚动观察，无法用仓库内静态检查替代。

## 最终结论

**不允许发布**

口径说明：
- 本次仓库内 P5 验收通过，未发现阻塞性的仓库内文档/脚本收口缺陷。
- 但发布放行条件并未满足，因为执行计划与 runbook 明确要求的真实线上 Step 1 到 Step 6 闭环尚未执行，也没有对应证据回填。
- 因此，当前“不允许发布”的原因不是仓库内质量失败，而是**强制线上验收未执行**；完成真实线上闭环并补齐证据后，可按同一报告口径复核放行。

- 通过项数：17
- 失败项数：0
- 待线上验证项数：11
- 遗留问题：仅剩真实线上执行与证据回填；仓库内未发现新增阻塞项
- 发布前必须处理的问题：严格按 `deploy/DEPLOY_RUNBOOK.md` 在线上完成 Step 1 到 Step 6，记录健康检查、systemd、应用日志、logrotate、journald 占用与恢复结果，再重新判定是否放行
